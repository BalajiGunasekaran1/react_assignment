function Header({title}){

    return(
        <>
        <p><strong>Q3.Create a component named "Header" that takes one prop - "title". The component should display a header
element with the given title</strong></p>
        <h1>{title}</h1>
        </>
    )
}

export default Header;